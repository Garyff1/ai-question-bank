# AI题库 v3 RC004 设备测试记录

## 当前结论

状态：**模拟器内部验证完成；安卓真机 Pending。**

本轮不把模拟器结果记为真机结果。Phase 2 Test002 的真实相机 OCR、真实扬声器 TTS、蓝牙/音频焦点和旧试卷 MP3 导出仍未验证。

## 模拟器

| 项目 | 结果 |
|---|---|
| 设备 | Android API 36 x86_64 模拟器 |
| 覆盖安装 | 成功，版本码 58，旧本地数据保留 |
| 冷启动 | 5.837 s（一次模拟器样本） |
| Motion Lab | 可进入；明确标记 INTERNAL 和本地 Fake 数据 |
| 知识炼成 | 按读取、规划、生成、校验顺序播放，真实数量到达后显示 5/5 |
| AI 模式传送门 | 可打开全屏门户层，演示不读取 API Key 且不产生支付 |
| 试卷装订 | 题卡层叠和装订准备状态正常，紧凑布局无溢出 |
| 减少动态/低性能 | 自动化测试通过；模拟器控件可切换，真机体感待测 |
| Logcat Fatal/ANR/OOM | 未发现，同时未发现 `E/flutter` |
| 图标完整性 | 初次 tree shaking 构建发现 Material 图标丢失；最终 APK 禁用图标 tree shaking 后复验通过 |

## 真机测试表

| 设备 | Android/API | 屏幕/性能 | 结果 | 帧率/发热/内存 | 问题 |
|---|---|---|---|---|---|
| 待连接 | 待测 | 待测 | Pending | Pending | Pending |
| 待连接 | 待测 | 待测 | Pending | Pending | Pending |

## 必测清单

1. AI 模式传送门连续切换 10 次，不丢失 API 配置。
2. 生成、取消、断网、失败重试与 20 题知识炼成。
3. 试卷装订、多页试卷、学生/教师版和 PDF 导出。
4. 单题抬起编辑、重新生成比较、拖动排序与答案层。
5. OCR 扫描镜、真实相机 OCR、TTS 与旧 MP3。
6. 中文/英文、浅色/深色、字体 1.3 倍、减少动态。
7. 连续页面切换、后台/锁屏恢复和配置变化。
8. Logcat、内存、发热、Fatal Exception、ANR 与 OOM。

## 发布边界

- 允许创建明确标记为内部测试的 GitHub Pre-release。
- 不合并 `main`，不替换 v2.9.9，不更新官网。
- 不开启真实微信、支付宝或真实扣款。
- 真机验收完成前不标记 RC004 为正式可发布版本。
